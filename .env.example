# .env.example
# This file serves as a template for your .env file.
# Copy this file to .env and fill in your actual secret values.
# The .env file is ignored by Git and should NEVER be committed.

### --- AI & External Services --- ###

# Your OpenAI API key. Used for generating embeddings.
# Get it here: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-...

# Your OpenRouter API key. Used as a gateway for various LLMs.
# This single key provides access to Google Gemini, Perplexity Sonar, etc.
# Get it here: https://openrouter.ai/keys
OPENROUTER_API_KEY=sk-or-...

# Your Firecrawl API key. Used for crawling and scraping websites.
# Get it here (the key is on the main dashboard after login): https://firecrawl.dev/
FIRECRAWL_API_KEY=fc-...

# Your LlamaCloud API key. Used for LlamaParse to extract text from PDFs.
# Get it here: https://cloud.llamaindex.ai/settings/api-keys
LLAMA_CLOUD_API_KEY=llx-...


### --- Vector Database (Qdrant) --- ###
# Connection details for your Qdrant instance.
# Create a free cluster here: https://cloud.qdrant.io/
QDRANT_HOST=https://your-qdrant-cluster-url.qdrant.tech:6333
QDRANT_API_KEY=your-qdrant-api-key


### --- Session & Task Queue (Redis) --- ###
# Connection details for your Redis-compatible database (e.g., Upstash, Aiven, Dragonfly).
# This is used for both Celery background tasks and user session storage.
# Example for Upstash (free tier available): https://console.upstash.com/redis
SESSION_DB_URL=rediss://user:password@hostname:port
# The SESSION_DB_TOKEN is often not needed if credentials are in the URL, but is kept for compatibility.
SESSION_DB_TOKEN=


### --- Application Configuration --- ###
# These are non-secret settings that can be configured here.

# Default number of FAQs to generate from content.
NUM_FAQS_TO_GENERATE=5

# Maximum number of pages to crawl on a website during ingestion.
MAX_CRAWL_PAGES=1

# The default LLM model to use for chat and FAQ generation via OpenRouter.
LLM_MODEL_DEFAULT=google/gemini-flash-1.5

# The LLM model to use for web search summarization via OpenRouter.
# This model is specifically tuned for search and reasoning.
WEB_SEARCH_MODEL_DEFAULT=perplexity/sonar-large-32k-online

# The OpenAI model used to create vector embeddings.
EMBEDDING_MODEL_DEFAULT=text-embedding-3-small

# The dimension of the embedding vectors. Must match the model used.
# text-embedding-3-small -> 1536
# text-embedding-3-large -> 3072
EMBEDDING_MODEL_DIMENSION=1536